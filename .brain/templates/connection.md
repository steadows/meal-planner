---
type: connection
features: [TODO, TODO]
kind: shared-file            # clash | shared-file | waiting-on | shared-rule  (LABEL ONLY — drives no logic)
status: watch               # open | watch | resolved
severity: medium            # high | medium | low
blocks: []                  # which feature(s) are stuck — empty if symmetric
files: []                   # trailing "/" = zone (prefix-match); else exact file
discovered: TODO
resolved: null
updated: TODO
---
TODO: what the edge is, who owns resolving it, what each side must do. Link both sides: [[a]] · [[b]].
